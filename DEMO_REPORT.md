# Repo Detective Report

**Health score:** 37/100  
**Files scanned:** 2  
**Detected stack:** Python

## Findings

- **CRITICAL** `possible-secret` at `app.py:1` — Possible hard-coded api_key
- **HIGH** `testing` at `project` — Source code exists, but no test files were detected
- **MEDIUM** `project-hygiene` at `project` — Add a README with setup and usage instructions
- **MEDIUM** `project-hygiene` at `project` — Choose and add a project license
- **MEDIUM** `project-hygiene` at `project` — Add a .gitignore before committing generated files
- **LOW** `work-marker` at `app.py:5` — TODO: add retry handling

## Recommended actions

1. Rotate exposed credentials, remove them from Git history, and use environment variables.
2. Add one smoke test for the most important user flow, then grow coverage around risks.
3. Add the missing repository hygiene files before inviting collaborators.
4. Triage TODO/FIXME markers into issues; delete stale or context-free markers.

---
Generated by Repo Detective Agent.
